"""Changepoint-detection diagnostic tool.

A pure, typed function the agent can call. Test-first against synthetic series with
KNOWN injected changepoints (precision / recall / FPR) before integrating with the agent.
"""
